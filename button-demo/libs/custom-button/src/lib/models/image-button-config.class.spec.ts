import { ImageButtonConfig } from './image-button-config.class';

describe('ImageButtonConfig', () => {
  it('should create an instance', () => {
    expect(new ImageButtonConfig()).toBeTruthy();
  });
});
