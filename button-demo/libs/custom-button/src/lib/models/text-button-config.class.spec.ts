import { TextButtonConfig } from './text-button-config.class';

describe('TextButtonConfig', () => {
  it('should create an instance', () => {
    expect(new TextButtonConfig()).toBeTruthy();
  });
});
